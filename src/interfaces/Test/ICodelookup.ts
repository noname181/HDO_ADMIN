export interface CLUInterface {
  id?: number | string;
  divCode: string;
  divComment: string;
  sequence: number;
  descVal: number;
  descInfo: string;
  use: boolean;
  subCodeCount: number;
  totalCount: number;
}
